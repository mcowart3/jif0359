version: 1
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    build:
      commands: [npm run buildWindows]
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /frontEnd/build
    files:
      - '**/*'
  cache:
    paths: []
